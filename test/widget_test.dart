import 'dart:convert';

import 'package:http/http.dart' as http;
import 'package:latihan/model/list_model.dart';

void main() async {
  // String url = "https://api.spaceflightnewsapi.net/v4/articles/?format=json";

  // var response = await http.get(Uri.parse(url));

  // var listResponse = jsonDecode(response.body);
  // var results = listResponse['results'];

  // var viewResults = NewsModel.fromJson(listResponse);

  // print(viewResults.results);
}
